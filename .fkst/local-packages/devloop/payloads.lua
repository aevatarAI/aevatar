return {
  shared = require("devloop.payloads.shared"),
  board = require("devloop.payloads.board"),
  builders = require("devloop.payloads.builders"),
  predicates = require("devloop.payloads.predicates"),
}
