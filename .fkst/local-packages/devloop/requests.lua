return {
  shared = require("devloop.requests.shared"),
  bodies = require("devloop.requests.bodies"),
  labels = require("devloop.requests.labels"),
  lifecycle = require("devloop.requests.lifecycle"),
  review = require("devloop.requests.review"),
}
