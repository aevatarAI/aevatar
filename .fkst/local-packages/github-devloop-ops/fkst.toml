kind = "package.composed"
name = "github-devloop-ops"
persistence_class = "saga"

[code]
root = "."

[lib_deps]
libraries = ["contract", "workflow", "testkit", "forge", "devloop"]

[event_deps]
packages = ["github-proxy", "github-devloop"]

[conformance]
function = "core.saga_conformance_errors"
