name: retrieve_facts
description: |
  Demonstrates the `retrieve_facts` primitive — keyword-based fact retrieval.
  Retrieves top-3 facts related to "programming language" from the input fact list.

roles: []

steps:
  - id: find_facts
    type: retrieve_facts
    parameters:
      query: "programming language"
      top_k: "3"
