name: maker_analysis_archived
description: Archived placeholder after maker_recursive removal.
roles: []
steps:
  - id: archived_notice
    type: emit
    parameters:
      event_type: maker_demo_archived
      payload: maker_recursive has been removed from the workflow runtime.
